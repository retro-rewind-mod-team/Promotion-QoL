-- ============================================================
--  Retro Rewind - Promotion QoL
--  CONFIGURATION FILE
--
--  flyerDelay      = ingame minutes to wait before giving a
--                    flyer to an NPC that spawns while the
--                    store is already open (default: 14)
--
--  conversionRate  = 0   → use native store popularity rate
--                    1–100 → override (percentage, 100 = all
--                    NPCs accept the flyer)
-- ============================================================

return {

    Debug          = false,

    flyerDelay     = 14,
    conversionRate = 0,

}
